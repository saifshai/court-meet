{
  "name": "court-meet",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://saifshai.github.io/court-meet-final-version",
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.3.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy-final-version": "gh-pages -b gh-pages-final -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "set-legacy-openssl": "export NODE_OPTIONS=--openssl-legacy-provider"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
